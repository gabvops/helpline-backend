version: '3.1'

services:
  db:
    image: mysql:8.0
    container_name: helpline-backend-db-1
    environment:
      MYSQL_ROOT_PASSWORD: Admin@2o24
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data: